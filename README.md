# Palan

## Build Instructions
1. Ensure you have a C++ compiler (g++), Flex, Boost and CMake installed.
2. Run `make` at the root of the repository to build the project. This will compile all source files,
    create the binaries in `build/bin`, and run all tests.

