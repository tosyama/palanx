cinclude <stdio.h>;

// `FILE` is a typedef for the incomplete `struct _IO_FILE`, so an owned
// declaration (which needs a layout) must be rejected. The
// diagnostic names the underlying tag, not the alias.
FILE f;
printf("unreachable\n");
