cinclude <stdio.h>;
cinclude <mcheck.h>;
type Point { int64 x; int64 y; };
type Polygon { [4]$Point pts; };
mtrace();
{
    Polygon poly;
    10 -> poly.pts[0].x;
    printf("%ld\n", poly.pts[0].x);
}
muntrace();
