cinclude <stdio.h>;
cinclude <mcheck.h>;
type Point { int64 x; int64 y; };
type Cluster { [2]Point pts; };
mtrace();
{
    Cluster c;
    @!int64 p = @!c.pts[0].x;
    99 -> p[0];
    printf("%ld\n", c.pts[0].x);
}
muntrace();
