uint32 m = 255;
int32 n = 3;
int64 big = -1;
uint32 mode = 8;

m & n;
n & m;
big & mode;
mode & big;
n < m;

func take_u32(uint32 x) -> uint32 {
    return x;
}
func take_i64(int64 x) -> int64 {
    return x;
}

take_u32(n);
take_i64(mode);
